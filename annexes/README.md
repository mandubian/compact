Annexes are runtime binding contracts (Compact F-5): conformance
declaration, enforcement register, role mapping — signed by the runtime's
identity key, verified at its boot, auditable offline. The annexes
themselves live in the runtime repos; this directory is the registry:

| Runtime | Annex | Standing | Status |
|---|---|---|---|
| dsh plugin composition (autonoetic-dsh) | lives in that repo | proposed (full) | drafting |
| autonoetic gateway | lives in that repo | retired with honors | — |
